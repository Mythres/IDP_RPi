# from enum import Enum

class MotorDirections:
    BACKWARD = 0
    FORWARD = 1
    LEFT = 0
    RIGHT = 1
    # # NormalPlane
    # NPDirection = Enum(BACKWARD=0, FORWARD=1)
    # # RotationalPlane
    # RPDirection = Enum(LEFT=0, RIGHT=1)
